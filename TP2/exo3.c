#include <stdio.h>


int main(int argc, char const *argv[])
{
    // a)

    // int i = 0, j = 0;
    // for (i = 2; i < 1000000; i *= 2) {
    //     j += i;
    // }    

    
    // b) 
    // for (int p = premier(); !est_dernier(p); p = suivant(p)) {
    //     traiter(p);
    // }

    // c)
    // for (; attendre(); );


    // // d)
    // int k=0;
    // for (i = 0, j = 0; ; i++) {
    //     j += i;
    //     if (j >= k)
    //         break;
    // }


    return 0;
}
